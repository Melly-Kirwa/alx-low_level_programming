#include "main.h"

/**
 * _islower - checks if letter is lowercase
 * @c: charater to check
 * Return: Always 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
