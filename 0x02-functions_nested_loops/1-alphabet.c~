#include "main.h"

/**
 * main - prints the alphabet in lowercase followed by new line
 *
 *Return: Always 0
 */
void print_alphabet(void)
{
	int i;

	for (i = 'a'; i < 'z'; i++)
	{
		_putchar('0' + i);
	}
	_putchar('\n');
	return;
}
